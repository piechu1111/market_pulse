market_pulse_dbt:
  target: dev
  outputs:
    dev:
      type: athena
      region_name: eu-central-1
      database: awsdatacatalog
      schema: market_pulse_dbt_ci_dev
      work_group: primary
      s3_staging_dir: s3://<BUCKET>/athena_results/dbt/
      s3_data_dir: s3://<BUCKET>/data/analytics/
      threads: 4